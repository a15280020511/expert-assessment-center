name: V5 R8J Stage-D Paid Blind Benchmark (Closed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - name: Closed paid trigger
        run: |
          echo "R8J is a consumed one-shot trigger."
          echo "It is closed after Run 30593349545 failed before inference because account credits could not cover the requested maximum token reservation."
          echo "A future attempt requires a new reviewed workflow and a unique owner-only command."
