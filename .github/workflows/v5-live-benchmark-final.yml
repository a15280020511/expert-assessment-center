name: Archived V5 R6 Economy Benchmark

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: R6 paid workflow disabled
        run: |
          echo "R6 is archived. A new R8 benchmark requires separate review and explicit manual unlock in Issue #64."
          echo "No OpenRouter secret is read and no model call is made."
