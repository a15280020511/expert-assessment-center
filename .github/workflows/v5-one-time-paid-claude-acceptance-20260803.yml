name: One-Time Paid Claude Acceptance 2026-08-03

on:
  pull_request:
    paths:
      - ".github/workflows/v5-one-time-paid-claude-acceptance-20260803.yml"

permissions:
  contents: read

jobs:
  paid-acceptance-disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "One-time paid acceptance is disabled after its first pre-call diagnostic failure."
