name: Archived V5 Legacy Micro Canary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy paid workflow disabled
        run: |
          echo "Disabled while V5 R8 remains a candidate. Canary activation requires the Issue #64 paid gate."
          echo "No OpenRouter secret is read and no model call is made."
