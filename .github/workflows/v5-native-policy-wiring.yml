name: V5 Native Policy Wiring

on:
  push:
    branches:
      - refactor/v5-explicit-runtime
    paths:
      - tools/apply_v5_native_policy_wiring.py
      - .github/workflows/v5-native-policy-wiring.yml

permissions:
  contents: write

concurrency:
  group: v5-native-policy-wiring
  cancel-in-progress: false

jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/v5-explicit-runtime
          fetch-depth: 0
      - name: Apply deterministic native wiring
        run: python tools/apply_v5_native_policy_wiring.py
      - name: Remove one-shot migration files
        run: |
          rm tools/apply_v5_native_policy_wiring.py
          rm .github/workflows/v5-native-policy-wiring.yml
      - name: Commit native wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor(v5): wire explicit planner and delivery policies"
          git push origin HEAD:refactor/v5-explicit-runtime
