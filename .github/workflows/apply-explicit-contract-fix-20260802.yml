name: Apply Explicit Contract Fix 2026-08-02

on:
  push:
    branches:
      - fix/v5-explicit-markdown-contract-20260802
    paths:
      - tools/apply_explicit_contract_fix_20260802.py
      - .github/workflows/apply-explicit-contract-fix-20260802.yml

permissions:
  contents: write

concurrency:
  group: apply-explicit-contract-fix-20260802
  cancel-in-progress: false

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out exact patch branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: fix/v5-explicit-markdown-contract-20260802
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-dev.txt

      - name: Apply exact code patch
        shell: bash
        run: |
          set -euo pipefail
          python tools/apply_explicit_contract_fix_20260802.py
          rm tools/apply_explicit_contract_fix_20260802.py
          rm .github/workflows/apply-explicit-contract-fix-20260802.yml

      - name: Verify patched files before commit
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check --no-input -r requirements-dev.txt
          python -m ruff check \
            open-model-market/v5_task_delivery_contract.py \
            open-model-market/v5_independent_artifact_revalidation.py \
            tests/test_v5_explicit_markdown_contract_revalidation.py
          python -m compileall -q open-model-market tests
          python -m unittest \
            tests.test_v5_explicit_markdown_contract_revalidation \
            tests.test_v5_long_markdown_contract_integrity \
            tests.test_v5_explicit_contract_pipeline_integrity -v

      - name: Commit patched source and remove temporary machinery
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "fix(contract): preserve explicit ordered Markdown delivery"
          git push origin HEAD:fix/v5-explicit-markdown-contract-20260802
