name: Archived V5 Legacy Low-Cost Pilot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy paid workflow disabled
        run: |
          echo "Disabled after R6. Use Issue #64 and a separately reviewed R8 paid-benchmark workflow."
          echo "No OpenRouter secret is read and no model call is made."
