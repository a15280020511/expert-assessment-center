# 专家研判中心最高宪法

本文件是专家研判中心的最高治理规则。代码、工作流、测试、维护、验收和生产执行均不得绕过本文件。任何冲突均按本文件处理，并以失败关闭。

## 第一条：免费优先测试，付费仅用于正式执行

1. 开发、维护、排错、回归和压力测试必须先执行零调用确定性测试，再执行实际费用严格为 `0.0 USD` 的免费模型 Canary。
2. 付费模型只允许用于正式生产任务，或用户明确授权的一次性最终付费验收。
3. 免费门未通过、目标 SHA 不一致、Artifact 不完整或未获授权时，禁止触发付费调用。
4. 付费失败后禁止自动重跑整个任务；只允许票据内预先批准的有限恢复调用。
5. 免费证据不得冒充正式模型身份验收，也不得单独移动 `production`。

## 第二条：专家模型公司必须全局不同

1. 同一任务内全部主力专家和热替补专家实行任务级全局 `all-different` 约束。
2. 品牌别名、子品牌和同一控制公司的模型必须归并为同一公司；Provider 不得改变公司归属。
3. 运行前验证计划公司唯一性，运行后根据全部实际调用重新审计；失败、超时、限流和恢复调用均计入。
4. 出现公司重复时失败关闭，不得通过 Provider 切换规避。

## 第三条：治理中心提供周榜前五十，专家中心使用 OR-Tools 确定性编组

当前唯一生产链为：

```text
任务与硬约束
→ 治理中心生成并签名 OpenRouter 推理周热度前 50 候选池
→ 模型身份、推理能力、价格和上下文元数据校验
→ 专家中心 OR-Tools CP-SAT 计算 4 个主力与 4 个热替补
→ 其余合格候选按同一目标函数保留为顺序替补
→ NetworkX DAG 校验
→ OpenRouter 对固定模型执行完全开放的 Provider 路由
→ 并行独立分析
→ 一次交叉审查
→ 一次最终综合
→ 审计、Artifact、独立复算、Attestation
```

1. 治理中心掌握候选池生成与签名权；专家中心不得越出治理中心签名的前 50 候选池补选模型。
2. 专家中心掌握候选池内的模型—角色—替补分配权，唯一允许的求解器为 OR-Tools CP-SAT。
3. OR-Tools 只执行确定性约束优化，不调用模型，不访问网络，不读取跨任务历史，不得演变为 Agent。
4. 组合必须同时满足：4 个角色槽完整、4 个热替补、模型全局唯一、公司全局唯一、模型级任务兼容、调用容量有界。
5. 目标函数只综合预计价格排名、过去一周使用热度排名和推理能力排名；Provider 数量、Provider 品牌、Provider 隐私属性、Provider 价格或 Provider 偏好不得参与专家模型编组评分。
6. 求解必须使用单工作线程、固定随机种子并要求 `OPTIMAL`；不能证明最优时失败关闭，不得退化为未审计贪心选择。
7. Claude 红队、GPT 选模和治理模型调用均关闭；模型循环、递归 Agent、自动扩团和备用治理运行时均禁止。
8. 旧前 20 选择器只作为显式回滚兼容路径，不得在已签名前 50 计划存在时抢占执行权。

## 第四条：热度窗口采用周榜

1. 主候选池固定采用过去一周 Token 使用量热度榜，窗口标识为 `week`，候选数量为 `50`。
2. 日榜波动过大，只能作为未来趋势告警信号，不得单独决定生产候选池。
3. 月榜反应过慢，只能作为未来稳定性参考，不得单独决定生产候选池。
4. 未经新的宪法变更，不得把日榜或月榜替换为主榜，也不得把多个窗口混合成不可审计的黑箱分数。

## 第五条：Provider 完全开放

1. Provider 路由完全交给 OpenRouter；专家中心、治理中心和运行时均不得设置 Provider 白名单、黑名单、固定顺序或单一 Provider 锁。
2. 请求不得携带 `provider.only`、`provider.order`、`provider.ignore`、ZDR 强制、数据收集筛选、Provider 价格筛选、量化筛选或其他会缩小 Provider 集合的路由约束。
3. 不得设置 `allow_fallbacks=false`；运行时不得通过 `require_parameters` 等 Provider 过滤字段间接缩小服务器池。
4. Provider 可以在 OpenRouter 当前可用 Provider 之间自由选择和故障转移；Provider 变化不视为模型替补。
5. 模型身份仍由专家计划固定。Provider 完全开放不允许 OpenRouter、运行时或恢复逻辑把请求替换成另一个模型。
6. Provider 不参与 OR-Tools 的模型评分、公司唯一性计算或替补排序；实际 Provider、费用、失败和响应元数据仍必须进入审计账本。
7. 发现任何 Provider 路由限制字段时，生产请求在模型调用前失败关闭，直到该限制被删除。

## 第六条：生产资格

只有同时满足以下条件才可移动 `production`：

- 目标 SHA 的零费用 CI 全部通过；
- 免费模型 Canary 通过且费用严格为零；
- 前 50 候选池、OR-Tools 最优性、公司唯一性、Provider 完全开放和禁止工具全部通过；
- 用户明确授权的正式付费验收通过；
- 独立 Artifact 复算通过；
- 最终 Attestation 与同一 Run、SHA、Artifact ID 和 Digest 绑定；
- 无未解决的阻断性审查线程。

任何一项缺失，生产资格均为 `BLOCKED`。

## 第七条：Token 与费用实行软治理，不得作为任务硬门

1. 不得以本地固定 Token 配额强制截断任务；模型原生上下文、接口协议和服务端能力可以作为客观兼容边界。
2. 费用与 Token 用于优化、提示、监测、告警、审计和回执，不得仅因超过本地费用阈值而把有效结果改判失败。
3. 价格指标必须基于预计任务调用成本，至少同时计算预计输入与预计输出成本，不得只比较单一输入单价。
4. 提示词必须先满足任务合同，再结论优先、证据优先、去重和提高信息密度。
5. 所有模型调用、Token、实际 Provider、费用和失败必须进入 Artifact。

## 第八条：具体问题具体分析，组织方式有界

1. 每个任务只使用当次任务、硬约束、证据包和治理中心签名候选池，不得沿用跨任务历史编组或历史权重。
2. 默认使用 `4` 个主力专家，角色固定为两名独立分析、一名交叉审查、一名最终综合；另计算 `4` 个热替补。
3. 其余合格候选不得丢弃，必须形成有序替补库存；实际恢复调用仍受票据批准的有限调用上限约束。
4. 专家组织固定为“并行独立分析 → 交叉审查 → 最终综合”，交叉审查和最终综合各一次。
5. 公司不足、候选池不完整、求解无最优解、图有环、工作覆盖不闭合或最终交付合同不满足时失败关闭。

## 第九条：Python 依赖保持最小充分集合

1. `jsonschema` 只负责合同与结构校验。
2. `NetworkX` 只负责有限 DAG 编排、拓扑校验和循环检测。
3. `OR-Tools CP-SAT` 只负责主力、角色、热替补和顺序替补的确定性组合优化。
4. 当前三者构成专家中心的最小充分运行依赖；没有可验证的新能力缺口时，禁止增加 Pandas、NumPy、SciPy、LangChain、CrewAI、AutoGen 或其他重复、重型框架。
5. 新依赖必须证明现有三者无法完成、没有重复功能、零费用测试通过且不会扩大模型工具权限。

## 第十条：专家执行全面禁止使用工具

1. 初始专家和恢复专家均不得调用外部工具。
2. 禁止浏览器、联网搜索、MCP、插件、代码执行器、Shell、数据库、文件检索、连接器、外部 API 及任何等效工具调用。
3. 专家只能使用本次任务票据、已提供证据包和同一任务依赖节点的合法输出。
4. 请求体不得暴露 `tools`、`tool_choice` 或函数调用字段；响应出现工具调用意图或不可审计外部来源时失败关闭。
5. 禁止工具是证据与隔离边界，不得以提高质量、降低成本或恢复失败为由绕过。
