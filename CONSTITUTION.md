# Expert Assessment Center Constitution

本文件是专家研判中心的最高宪法。生产代码、工作流、测试、维护脚本、验收流程和文档与本文件冲突时，以本文件为准；任何例外必须由仓库所有者以一次性、不可变、可审计授权明确给出，不得通过默认值、fallback、兼容层或人工口头判断绕过。

## 第一条：治理链唯一

正式生产治理链固定为：

```text
GPT latest 提案一次
→ Claude Opus latest 统一红队建议一次
→ GPT latest 综合一次
→ 确定性宪法校验器
→ 专家执行
→ 原生证据、Manifest、Artifact、独立复算和最终回执
```

Claude 只提供修改建议，不得批准、否决、放行、阻断、选择专家、执行任务或触发第二轮红队。确定性校验器是唯一硬门。

## 第二条：免费模型优先，付费模型仅用于正式生产

测试、开发、维护、调试、回归、压力验证、协议验证、提示词验证、Schema 验证、请求封装验证、证据链验证和故障定位，必须遵循以下顺序：

```text
零模型调用的确定性验证
→ 免费模型影子验证
→ 独立 Artifact 复算
→ 仅在正式生产资格或正式生产任务需要时，使用付费生产模型
```

硬性规则：

1. 能由静态检查、单元测试、fixture、模拟响应或确定性回放完成的验证，不得调用任何模型。
2. 必须调用模型才能验证的测试和维护任务，优先且默认只允许免费模型或账户内明确包含的免费额度。
3. 免费模型测试必须明确记录模型、公司、Provider、调用次数和费用边界；不得静默切换到付费模型。
4. 免费路径不可用、额度耗尽、隐私策略不兼容或能力不足时，必须失败关闭；不得自动升级为付费调用。
5. 付费模型只允许用于：正式生产任务、正式生产模型身份兼容性验收、或仓库所有者明确授权的一次性最终验收。
6. 付费验收前必须已经完成同一目标 SHA 的零调用验证和免费模型影子验证；证据必须可追溯到同一提交、同一运行和同一 Artifact。
7. 免费模型影子验证只能证明协议、链路、解析、物化、执行和证据系统可用，不能冒充或替代 GPT latest / Claude Opus latest 的正式模型身份资格。
8. 所有付费调用必须显式授权、限制最大调用次数、最大恢复次数和费用上限，并禁止隐式重试、隐式 fallback 与跨模型自动升级。

## 第三条：专家团模型公司全局唯一

同一任务内所有实际参与专家团工作的模型，必须来自不同公司。

硬性规则：

1. 初始专家节点之间，模型公司全局唯一。
2. 恢复专家与初始专家之间，模型公司全局唯一。
3. 不同模型名称、不同版本、不同 Provider 或不同端点，只要归属同一模型公司，仍视为同一公司，禁止重复使用。
4. 已失败的专家调用也计入公司占用，防止通过失败重试绕过公司唯一性。
5. OpenAI 与 Anthropic 固定承担治理角色，不得进入专家候选池。
6. 实际调用账本必须重新计算公司集合；计划图合规但实际调用发生公司重复时，最终状态必须失败关闭。
7. 当可用公司数量不足以满足任务图时，不得降低约束、复用公司或切换到同公司其他模型，必须返回不可行。

## 第四条：任务无关与动态决定

生产系统不得为任何具体测试题、行业、案例或关键词预设固定专家、固定职业、固定节点数、固定拓扑、固定模型组合、固定质量权重或特例分支。能由当次任务和当次模型目录决定的变量，必须在当次治理过程中动态决定。

## 第五条：工具与中心隔离

专家执行禁止工具、浏览器、插件、文件搜索、代码执行和外部网络。专家中心不得直接连接其他业务中心；GPTs 是唯一控制和证据中继。

## 第六条：证据与晋级

任何生产资格必须同时满足：

- 权威源码 SHA 明确且不可变；
- 零费用验证通过；
- 免费模型影子验证通过；
- 正式模型身份验收在需要时通过；
- 原生证据完整；
- 主 Artifact 上传成功；
- 独立 Job 从原始 Artifact 重新计算通过；
- 最终 Attestation 与同一 Run、同一 SHA、同一 Artifact digest 绑定。

任一条件缺失时，PR 保持 Draft 或生产资格保持 `BLOCKED`，不得移动 `main` 或 `production`。

## 第七条：失败关闭

任何不确定、缺失、冲突、无法验证或外部策略阻断，均按失败处理。不得以“预计可用”“大概率通过”“免费模型已通过”等推断替代正式证据。
