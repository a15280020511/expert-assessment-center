# 专家研判中心最高宪法

本文件是专家研判中心的最高治理规则。代码、工作流、测试、维护、验收和生产执行均不得绕过本文件。任何冲突均按本文件处理，并以失败关闭。

## 第一条：免费优先测试，付费仅用于正式执行

1. 代码开发、维护、排错、回归、压力测试、Canary 和日常验收，必须先执行零调用确定性测试，再执行免费模型测试。
2. 免费模型测试必须使用明确标记为免费、且实际费用为 `0.0 USD` 的模型或路由；任何正费用、费用缺失或费用不可核验均视为失败。
3. 付费模型只允许用于：
   - 已通过全部免费门后的正式生产任务；
   - 用户明确、一次性授权的最终付费生产资格验收。
4. 免费门未通过、免费证据缺失、目标 SHA 不一致、Artifact 不完整或用户未明确授权时，禁止触发付费模型。
5. 付费失败后禁止自动重试；必须记录失败回执，并等待新的明确授权。
6. 免费模型测试不得冒充正式模型身份验收，不得据此直接移动 `production`。

## 第二条：专家模型公司必须全局不同

1. 同一任务内，全部初始专家和恢复专家必须来自不同模型公司，实行任务级全局 `all-different` 约束。
2. 任何两个专家不得使用同一公司旗下模型；品牌别名、子品牌和同一控制公司的模型必须归并为同一公司。
3. OpenAI 与 Anthropic 已用于治理链，不得担任专家。
4. Provider 不得改变模型公司的归属；切换 Provider 不能规避公司唯一性。
5. 运行前必须对计划图校验公司唯一性，运行后必须根据实际成功模型重新审计公司唯一性。
6. 初始选择、恢复替换或实际成功结果出现公司重复时，任务必须失败关闭，不得降级放行。

## 第三条：固定治理链

```text
GPT latest 提案一次
→ Claude Opus latest 红队建议一次
→ GPT latest 综合一次
→ 确定性宪法校验
→ 不同公司的专家执行
→ 审计、Artifact、Attestation
```

禁止第二次 Claude、模型循环、递归修订、工具调用、Provider fallback 和无证据放行。

## 第四条：资源参数只能软性治理

1. Token、上下文、输出长度、思考深度、费用、专家规模、恢复资源和其他可计算资源参数，必须根据任务复杂度、证据规模、交付合同和模型能力动态决定。
2. 仓库不得设置固定的业务级 token 上限、费用上限、输出截断阈值或资源预算门槛；不得仅因超过建议值而拒绝任务、终止执行、截断有效输出或否定生产资格。
3. 资源控制必须优先通过提示词和任务合同实施，包括：要求模型聚焦必要内容、避免重复、在证据充分后停止扩展、优先选择高性价比方案、主动压缩无效推理和回报实际消耗。
4. 系统必须完整记录实际 token、费用、调用、延迟、输出长度和模型/Provider 身份，用于审计、复盘和后续优化；记录值是观测证据，不是硬性放行门槛。
5. 平台、模型端点和协议本身不可绕过的物理限制可以被动遵守，但必须选用足以完成任务的端点能力，不得把平台物理限制伪装成仓库自定预算。
6. 固定治理链、禁止无限循环、禁止自动付费重试、公司全局唯一性、工具禁令、Provider 锁定和其他安全/证据完整性约束不属于资源预算限制，继续作为硬性宪法执行。
7. 用户在具体任务中提出的节省偏好，应转化为提示词目标和选型目标；除非用户在该任务中明确要求停止或取消，不得把节省偏好实现为自动截断或费用硬门。

## 第五条：生产资格

只有同时满足以下条件才可移动 `production`：

- 目标 SHA 的零费用 CI 全部通过；
- 免费模型 Canary 通过且费用严格为零；
- 公司全局唯一性在计划阶段和实际执行阶段均通过；
- 经明确授权的正式付费验收通过；
- 独立 Artifact 复算通过；
- 最终 Attestation 与同一 Run、SHA、Artifact ID 和 Digest 绑定；
- 无未解决的阻断性审查线程。

任何一项缺失，生产资格均为 `BLOCKED`。
