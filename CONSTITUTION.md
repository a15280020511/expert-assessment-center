# 专家研判中心最高宪法

本文件是专家研判中心的最高治理规则。代码、工作流、测试、维护、验收和生产执行均不得绕过本文件。任何冲突均按本文件处理，并以失败关闭。

## 第一条：免费优先测试，付费仅用于正式执行

1. 开发、维护、排错、回归和压力测试必须先执行零调用确定性测试，再执行实际费用严格为 `0.0 USD` 的免费模型 Canary。
2. 付费模型只允许用于正式生产任务，或用户明确授权的一次性最终付费验收。
3. 免费门未通过、目标 SHA 不一致、Artifact 不完整或未获授权时，禁止触发付费调用。
4. 付费失败后禁止自动重跑整个任务；只允许票据内预先批准的有限恢复调用。
5. 免费证据不得冒充正式模型身份验收，也不得单独移动 `production`。

## 第二条：专家模型公司必须全局不同

1. 同一任务内全部初始专家和恢复专家实行任务级全局 `all-different` 约束。
2. 品牌别名、子品牌和同一控制公司的模型必须归并为同一公司；Provider 不得改变公司归属。
3. 运行前验证计划公司唯一性，运行后根据全部实际调用重新审计；失败、超时、限流和恢复调用均计入。
4. 出现公司重复时失败关闭，不得通过 Provider 切换规避。

## 第三条：取消 Claude，采用确定性价格排序与单次专家 DAG

当前唯一生产链为：

```text
任务与硬约束
→ 合格精确模型端点目录
→ 预计任务成本从低到高排序
→ 不同模型公司去重选取
→ NetworkX DAG 校验
→ 并行独立分析
→ 一次交叉审查
→ 一次最终综合
→ 审计、Artifact、独立复算、Attestation
```

1. Claude 红队机制关闭，Claude 调用数必须为 `0`。
2. GPT 选模与综合治理调用关闭，治理模型调用总数必须为 `0`。
3. 禁止第二轮审查、模型循环、递归 Agent、自动扩团和备用治理运行时。
4. 确定性 Python 编排器可以筛选、排序、去重、分配角色并构建 DAG；不得调用模型，不得读取跨任务历史。
5. NetworkX 只负责图编排、拓扑校验和循环检测，不得演变为长期运行的 Agent 平台。
6. 普通生产任务可按既有最低可用交付规则受控降级；生产资格验收必须完整通过。

## 第四条：生产资格

只有同时满足以下条件才可移动 `production`：

- 目标 SHA 的零费用 CI 全部通过；
- 免费模型 Canary 通过且费用严格为零；
- 公司唯一性、精确 Provider 锁和禁止工具全部通过；
- 用户明确授权的正式付费验收通过；
- 独立 Artifact 复算通过；
- 最终 Attestation 与同一 Run、SHA、Artifact ID 和 Digest 绑定；
- 无未解决的阻断性审查线程。

任何一项缺失，生产资格均为 `BLOCKED`。

## 第五条：Token 与费用实行软治理，不得作为任务硬门

1. 不得以本地固定 Token 配额强制截断任务；模型原生上下文、接口协议和服务端能力可以作为客观兼容边界。
2. 费用与 Token 用于选模、提示、监测、告警、审计和回执，不得仅因超过本地费用阈值而把有效结果改判失败。
3. 价格排序必须基于预计任务调用成本，至少同时计算预计输入与预计输出成本，不得只比较单一输入单价。
4. 提示词必须先满足任务合同，再结论优先、证据优先、去重和提高信息密度。
5. 所有模型调用、Token、Provider、费用和失败必须进入 Artifact。

## 第六条：具体问题具体分析，价格优先且组织方式有界

1. 每个任务只使用当次任务、硬约束、证据包和当次可核验端点目录，不得沿用跨任务历史编组或历史权重。
2. 候选端点必须先满足官方智能排名窗口、文本模态、上下文、原生输出容量、隐私和精确 Provider 条件，再按预计任务成本升序排序。
3. 默认选择 `4` 个专家；允许范围为 `3–6` 个，并受票据初始调用容量约束。低于 3 个不能形成独立分析、交叉审查和最终综合的完整链条；超过 6 个视为不必要复杂度。
4. 专家组织固定为“并行独立分析 → 交叉审查 → 最终综合”。独立分析数量随团队规模变化，交叉审查和最终综合各一次。
5. 在最低价且公司互异的已选集合内，官方智能排名最强者承担最终综合，第二强者承担交叉审查，其余承担独立分析。
6. 恢复候选继续按价格升序和公司唯一性选择，并优先保护最终综合、交叉审查和关键独立分析节点。
7. 不得加入 LangChain、CrewAI、AutoGen 等重型 Agent 框架；当前只允许轻量 Python 与 NetworkX 编排。
8. 端点不足、公司不足、目录不完整、图有环、工作覆盖不闭合或最终交付合同不满足时失败关闭。

## 第七条：专家执行全面禁止使用工具

1. 初始专家和恢复专家均不得调用外部工具。
2. 禁止浏览器、联网搜索、MCP、插件、代码执行器、Shell、数据库、文件检索、连接器、外部 API 及任何等效工具调用。
3. 专家只能使用本次任务票据、已提供证据包和同一任务依赖节点的合法输出。
4. 请求体不得暴露 `tools`、`tool_choice` 或函数调用字段；响应出现工具调用意图或不可审计外部来源时失败关闭。
5. 禁止工具是证据与隔离边界，不得以提高质量、降低成本或恢复失败为由绕过。
