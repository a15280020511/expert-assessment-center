# 专家研判中心最高宪法

本文件是专家研判中心的最高治理规则。生产代码、工作流、测试、维护和验收不得与本文件冲突。机器策略 JSON、提示词、运行时门禁与测试均是本文件的派生实现，不得形成第二套最高规则。

## 第一条：唯一硬模型边界是禁止工具

1. 初始专家、审查专家、综合专家、恢复专家和任何替补模型均不得调用外部工具。
2. 禁止 `tools`、`tool_choice`、函数调用、浏览器、联网搜索、MCP、插件、代码执行器、Shell、数据库、文件检索、连接器、外部 API、Computer Use 及等效能力。
3. 模型只能使用当前票据、当前证据包及同一任务 DAG 中合法前序节点的输出。
4. 请求在发送前检查工具字段；响应出现工具调用、函数调用或不可审计外部检索证据时失败关闭。
5. 禁止工具是唯一的**模型资格硬边界**，不得因质量、费用、恢复或便利性绕过。

## 第二条：治理中心只提供候选源，不替专家中心选模

1. 治理中心从 OpenRouter 当前模型目录取得**支持 reasoning、文本输出、按 `most-popular` 使用热度排序**的完整候选序列。
2. 候选序列不得固定截断为 Top20、Top50 或任何固定 Top-N；历史 `top50_*` 字段仅允许作为兼容别名。
3. 治理中心负责候选身份、任务绑定、传输哈希和完整性；不得按价格、旗舰、公司、Provider、ZDR、免费/付费、上下文阈值、Canary 或历史成功率过滤模型。
4. 推理能力、热度、价格、上下文和输出容量均作为专家中心动态计算的元数据，不作为治理中心的模型准入门槛。
5. 治理中心必须在计划中声明 `tool_use_forbidden=true`、`tools_allowed=false`、`only_hard_model_boundary=no-tools`。

## 第三条：所有可由当前任务计算的编组与执行参数都必须动态计算

1. 每个任务只依据当前票据、当前证据、当前治理候选池和当前运行反馈重新计算，不读取跨任务历史编组或固定领域路由。
2. 至少动态计算：任务体量、证据体量、要求数量、验收数量、交付数量、输入/约束/证据/交付压力、提示 Token 估计、输出 Token 估计、协议预留、依赖 fan-in、角色上下文需求、团队规模、角色拓扑、恢复规模、恢复顺序、角色权重、模型—角色分配、预计任务费用、推理能力权重、热度权重、容量风险、边际回报、求解时间、可复算随机种子，以及当前请求需要的 reasoning effort、输出 transport allowance、**有效模型 timeout**、恢复深度等可计算运行参数。
3. 参数生命周期必须形成闭环：**任务建模 → 决策发现 → 参数设计 → 参数实例化 → 求值 → 绑定 → 消费 → 观测 → 当前运行反馈重算**。仅生成参数、只设计不求值、或只写入审计而没有进入真实执行，均不算动态适配完成。
4. 参数设计本身必须被审计。对每个当前任务发现的参数，至少要明确其 `value_type`、`domain`、`resolver`、参数依赖、consumer binding 和 recompute trigger；每个设计维度必须标记为 `constitutional_invariant`、`infrastructure_invariant`、`current_task_derived` 或 `current_run_feedback_derived`，不得把基础设施不变量伪装成动态参数。
5. 任务简单时提高成本与边际回报的重要性；任务复杂、约束高、证据多、交付复杂时动态提高推理能力与容量余量的重要性。
6. 禁止用“法律”“商业”“数学”等领域关键词直接指定模型、公司、固定职业角色或固定团队模板。
7. 不得固定 4+4、固定专家人数、固定审查人数、固定恢复人数或固定角色拓扑；团队和 DAG 必须由当前任务形状生成。
8. Token、费用、余额与调用数实行软治理：不得以固定本地 Token 上限、固定费用阈值、固定余额阈值或固定调用数量作为模型准入、任务拒绝或有效输出否决门槛；这些信息用于动态规划、请求整形、软目标、告警、遥测和审计。
9. 动态计算必须同时覆盖**时间维度与空间维度**并在当前运行中持续发生：时间维度至少在每次真实模型尝试、每次质量门观测、每次失败分类以后更新当前运行状态；空间维度至少在每次 retry/replacement 前根据当前节点在有限 DAG 中的位置、剩余初始恢复候选、standby 候选空间及本次运行已失败/已尝试身份重新计算。不得把“任务开始时算一次”或“初始化恢复顺序后机械执行到底”称为持续动态适配。
10. 持续动态重算不得突破有限 DAG 与有限候选空间这一安全不变量。可以重算尚未消费的模型、恢复/standby 顺序、请求参数和资源分配，但不得形成无限 Agent 循环、无界递归重试或跨任务自我延续状态。

## 第四条：OR-Tools 负责动态组合，但最优性不是门禁

1. OR-Tools CP-SAT 是主组合求解器；其输入由当前任务动态画像和当前候选元数据生成。
2. OR-Tools 可以返回 `OPTIMAL` 或 `FEASIBLE`；无法及时得到可接受状态时允许使用同一当前任务目标函数的确定性启发式回退。
3. `OPTIMAL` 证明不得作为执行资格门禁。
4. 求解器不得加入公司唯一、Top-N 成员、价格上限、免费优先、旗舰、Provider、ZDR、预算或容量兼容等模型资格硬约束。
5. 模型原生上下文/输出容量不足应进入动态风险和目标函数；若服务端客观无法执行，运行时据实际错误进入恢复，不把本地估计伪装成资格门禁。
6. 同一精确模型身份不得同时占用多个执行节点，仅用于保证执行图身份一致性；这不是公司或模型类别门禁。
7. 单工作线程可以保留用于可复算审计；求解时间和随机种子应由当前任务与问题规模动态导出。

## 第五条：Provider 完全开放

1. Provider 路由完全交给 OpenRouter，不设置 `provider.only`、`provider.order`、`provider.ignore`、ZDR、数据收集、价格、量化或其他 Provider 筛选。
2. OpenRouter 可以为同一模型身份自由选择 Provider 并进行 Provider 故障转移。
3. OpenRouter Provider 路由不得擅自把一个计划模型替换成另一模型；跨模型替补权只属于专家中心的当前任务恢复图。
4. Provider 品牌、数量、价格、隐私标签不得进入专家模型编组评分；实际 Provider、费用、Token、失败和响应元数据仍进入审计。

## 第六条：除禁止工具外，模型业务门禁全部开放；资源只做软治理

以下项目不得作为模型准入或执行门禁：

- Top20 / Top50 / 固定 Top-N；
- 公司唯一或公司数量；
- 旗舰/非旗舰；
- 价格阈值或价格排序资格；
- 免费优先、免费 Canary、零费用证据；
- intelligence rank 门槛；
- Provider endpoint、ZDR 或 Provider 数量；
- OR-Tools 必须 `OPTIMAL`；
- 固定团队规模、固定角色、固定恢复数；
- 固定预算、固定 Token、固定调用上限；
- 跨任务历史成功率、失败率或历史模型偏好。

这些因素可以作为当前任务的**软目标、风险、请求整形、遥测或审计信息**，但不得拒绝一个治理中心已经提供的候选模型。

允许发送由当前请求动态计算的 `max_tokens` / 等效 transport allowance，用于避免 Provider 按模型原生最大输出做最坏情况余额预留；它不是任务预算、模型资格门禁或有效结果否决门槛。若实际出现截断，应由当前运行反馈提高 allowance 或切换恢复路径，而不是把固定额度作为停止理由。

模型调用 timeout 的**有效值必须由当前请求形状和当前运行反馈动态计算**。允许保留有限的安全上限作为防止无限等待的基础设施不变量，但该安全上限不是模型业务门禁；真实超时后只允许基于本次运行反馈重算有效 timeout，且不得越过有限安全上限。

## 第七条：完整性和安全校验不是模型业务门禁

1. 任务身份、票据结构、SHA/Artifact/候选池传输完整性必须可验证，防止执行错误任务或被篡改的数据。
2. 执行图必须有限且无环；禁止无限模型循环、递归 Agent 和无界重试。
3. 密钥、认证、仓库隔离和控制面权限必须保持安全。
4. 专家模型网络面只允许访问 OpenRouter 模型接口；GitHub 仅属于控制面。模型本身不得获得搜索、浏览或其他外部网络工具。
5. 当前模型若已从 OpenRouter 目录消失、服务端拒绝请求或实际调用失败，应进入动态恢复流程，而不是放松完整性或工具边界。

## 第八条：动态恢复只使用当前运行反馈

1. 恢复规模由当前任务压力、结构广度和候选池规模动态计算。
2. 恢复模型按当前任务的成本、能力、热度、容量风险和边际回报重新评分，不设置公司/免费模型/Provider 固定惩罚。
3. 主模型失败、超时、服务端不可执行或结果质量不足时，可以从动态恢复池替换模型。
4. 恢复不得赋予模型工具、浏览、函数调用或额外外部数据能力。
5. 当前运行中已出现**明确不可重试**的模型身份/端点失败时，必须记入仅本次任务有效的失败记忆；后续节点不得再次为同一明确失败身份付费尝试。该记忆不得跨任务保存。
6. 运行时 promotion depth、候选过滤与可用顺序应依据当前失败类别和当前运行反馈重算；不得把跨任务历史失败率固化为偏好。
7. 若失败原因是输出截断，应依据本次实际 completion / allowance 压力重算下一请求输出余量；在跨模型替换前，应优先对同一模型做一次基于本次截断反馈的请求形状修复重试；若失败原因是 timeout，应依据本次实际耗时 / 有效 timeout 压力重算下一请求 timeout；不得以固定业务常数替代当前运行反馈。
8. 恢复与 standby 的公司异质性遵循第十五条：能力与本次失败风险优先，在这些信号不受损的可选空间中优先尚未尝试的公司，避免无必要的连续同公司替补。
9. 当前节点一旦通过实际运行证明某个输出 allowance 或有效 timeout 不足，该失败证据必须形成**同节点、同当前运行**的动态下限/压力状态并传递给后续替补模型；换模型不得无理由把已证明不足的参数重新降回原值。该状态只在本次任务内有效，任务结束即丢弃。
10. 初始 recovery 列表不是执行期间的静态命令序列。每次 retry/replacement 前，系统必须把剩余初始恢复候选与当前可提升 standby 视为新的有限候选空间，结合最新失败类别、当前节点时空压力、已尝试身份和软异质性目标重新排序后再选择下一次调用。

## 第九条：最小充分依赖

1. `jsonschema`：协议与结构校验。
2. `NetworkX`：有限 DAG 编排、拓扑检查和循环检测。
3. `OR-Tools CP-SAT`：当前任务的模型—角色—恢复组合优化。
4. `Optuna`：仅用于当前任务低维、有限搜索空间中的参数形状/恢复广度求解；搜索空间、试验数和种子必须由当前任务生成，不得成为模型资格门禁，并必须保留确定性当前信号回退。
5. 任务画像、Token/费用估计、动态权重、边际回报和求解参数计算优先使用 Python 标准库；无明确能力缺口时不增加重型 Agent/编排框架。
6. 运行时依赖清单必须与本条一致；新增依赖必须先证明现有四类依赖无法以更简单、稳定方式完成同一能力，并同步修改本宪法、机器策略和 CI。

## 第十条：审计真实性

1. Artifact 必须记录实际候选源、动态参数、参数设计、团队形状、角色计划、OR-Tools/回退状态、选中模型、恢复模型、实际调用、Token、费用、Provider、失败和最终交付状态。
2. 审计字段不得保留会反向覆盖真实状态的旧语义；例如专家中心已动态选模时不得再报告 `selection_authority=decision-system-governance`，允许动态恢复时不得报告 `model_substitution_allowed=false`。
3. 所有“门禁关闭”“全部参数动态计算”“禁止工具”声明必须能由运行时证据复核，而不能只存在于文档或配置中。
4. 报告不得把实际读取了上游节点结果的节点称为“独立意见”；独立性必须与 DAG 可见边一致。
5. 多模型运行必须审计实际公司序列、不同公司数量、公司异质性比例、连续同公司替补次数以及首次重复前覆盖的不同公司数；这些字段是可观察性与优化反馈，不得反向成为模型资格门禁。
6. 启用持续时空动态计算时，Artifact 必须至少能复核 `current_run_replan_epoch`、每节点当前运行反馈状态、时空压力、重算触发点、候选空间重排事件，以及截断/timeout 学习值是否跨同节点替补持续生效；不得只用一个 `dynamic=true` 布尔值代替执行证据。

## 第十一条：Runtime Knob Coverage 是生产验收项

1. 所有会改变实际模型执行行为的参数必须被分类为：`constitutional_invariant`、`infrastructure_invariant`、`current-task-derived` 或 `current-run-feedback-derived`；不得存在来源不明的隐藏运行旋钮。
2. 对每个动态参数必须能够审计 `planned_value → effective_value → consumer/request_field`；对运行时派生 timeout 等参数，至少必须审计 `derived_value → effective_value → actual_attempt`。
3. `computed-but-unused`、计划值与实际请求值不一致、实际调用缺少动态 timeout 绑定、参数设计维度未分类、或声明动态但仍由未登记固定常数控制时，Runtime Knob Coverage 必须标记为 `FAIL`。
4. Runtime Knob Coverage 失败不允许伪称“全部参数动态适配完成”；可以保留已经产生的有用业务输出，但生产验收不得记为参数闭环通过。
5. `runtime_knob_coverage` 必须由 Artifact 打包前的最后一个请求审计写入者保留；任何后续兼容层不得覆盖或删除该生产验收证据。

## 第十二条：相关性不等于依赖

1. 语义相似、主题相关、共享关键词只允许作为**软 relatedness 信号**，不得自动生成阻塞执行的 DAG dependency。
2. 硬依赖只能来自票据显式依赖、某工作项确实需要消费另一工作项的输出，或最终综合节点对各独立分支的汇聚。
3. 能并行的工作必须保持并行，以降低时延并保留真正独立的反证价值；不得为了方便编排把全部角色串成伪独立链。

## 第十三条：质量门必须基于可观察契约，不得用隐藏固定启发式制造恢复

1. 回答长度不得作为质量的固定硬代理；不得因固定 120/320 字之类阈值触发替补模型。
2. 不得用固定业务权重系数或固定数值质量阈值把一个已满足可观察契约的回答判为失败并额外调用模型。
3. 质量硬失败只能来自可复核信号，例如：空输出、明确截断、拒绝交付、必填契约缺失、必须 JSON 却语法/结构非法、最终证据或精确数量校验失败。
4. 数值质量分可以保留为等权或当前任务派生的遥测，但不得以隐藏固定阈值成为恢复门禁。
5. 内部节点仅发生保守自然语言事实标签匹配误差时可以降为带审计警告的内部结果；最终节点的事实、证据、数量与显式交付契约仍必须 fail-closed。
6. 最终质量门必须从**当前任务显式要求**动态编译必答项；标题、字段名或章节存在不等于该必答项已经完成。用户明确要求计算、临界值、敏感性、误差情景、多目标建议或事实/计算/判断分类时，对应内容为空或没有实质结果必须失败关闭。
7. 模型主动展示的确定性算术与线性临界值方程属于可观察输出，可用标准库做数学一致性校验；显式等式与其声称结果不一致时不得进入 `full_success`。该校验不读取隐藏思维链，也不使用固定业务质量分。
8. 封闭世界任务中，题面外的精确数量若由题面权威输入通过局部可复核算式或派生关系得到，应保留并标记为 `derived_from_authoritative_input`；确定性清洗不得把合法派生值误删。无法证明来源的新增外部数量继续 fail-closed。

## 第十四条：参数设计必须先于参数求值

1. 系统必须先根据当前任务建立 Work DAG，再发现当前任务需要解决的决策自由度；不得先拿固定业务参数表套任务。
2. 决策发现以后、数值求解以前，必须生成当前参数的设计证据，回答“为什么需要这个参数、它是什么类型、可行域如何形成、由什么方法求解、依赖谁、由谁消费、什么变化触发重算”。
3. 设计中属于协议、安全、数学或基础设施的固定边界必须明确登记为不变量；属于当前任务或当前运行反馈的部分必须由当前信号生成。不得为了满足“全部动态”口号而把不变量伪造为动态值。
4. `Decision → ParameterDesign → ParameterSpec → ResolvedValue → RuntimeBinding` 必须可在同一 Artifact 中追溯；缺任一环节，参数闭环验收为 `FAIL`。
5. 当前任务若不需要某个控制面，不得为填满固定参数目录而强行生成该参数；参数实例数量必须服从当前决策集合。

## 第十五条：多模型异质性优先，但不是公司硬门禁

1. 当当前任务需要多个专家、恢复模型或 standby promotion 时，应**尽可能提高不同公司 AI 的覆盖**，以降低同源模型偏差、共同失败模式和单一模型家族盲点。
2. 异质性是任务质量与本次运行可靠性之后的**软/次级优化目标**：能力匹配和本次失败风险优先；在这些更高优先级信号相同或不受损时，优先尚未使用的公司，再以成本和模型身份等软信号排序。
3. 禁止把“必须 N 家公司”“每家公司只能一个模型”“公司重复即拒绝”等规则重新变成准入或执行硬门禁。同公司模型在明显更适配、可用性更高、或不同公司候选不足时可以使用，但必须保留原因与实际序列审计。
4. 当前运行恢复顺序应维护公司使用序列；只要存在更高优先级信号相当的未尝试公司，就应避免连续提升同一公司模型。不同公司的候选耗尽或高优先级信号明显更差后，可以回到同公司候选。
5. 公司异质性不得读取跨任务历史偏好，只使用当前候选池、当前任务评分和当前运行反馈；Artifact 必须能复核这一点。
