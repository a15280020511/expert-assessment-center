# 专家研判中心最高宪法

本文件是专家研判中心的最高治理规则。代码、工作流、测试、维护、验收和生产执行均不得绕过本文件。任何冲突均按本文件处理并失败关闭。

## 第一条：职责边界与唯一调用链

专家中心不再拥有模型选择权、任务拆解权或跨中心调度权。唯一合法调用链为：

```text
用户
→ 网页 GPT（唯一语义总控，形成任务与显式工作计划）
→ 治理中心（按实际任务预计费用选择并签发模型名单）
→ 专家中心（验证名单并用 NetworkX 组织 DAG）
→ 名单内专家执行
→ Artifact、独立复算和 Attestation
→ 网页 GPT 汇总
```

1. 网页 GPT 负责根据具体问题定义工作项、角色、依赖关系、必需输出和最终综合工作。
2. 治理中心负责读取当次 OpenRouter 模型目录和基准，在全部合格的付费通用旗舰模型中按实际任务预计费用从低到高选择若干模型，并签发不可变名单。
3. 专家中心只负责验证治理名单、解析名单模型的合格精确端点、构建执行图、执行、审计和回执；不得重新选模、增加模型、替换模型公司或重新拆解任务。
4. 情报、计算和专家中心之间不得直接通信；任何跨中心衔接只能经过网页 GPT 与治理中心。
5. Claude 红队机制、GPT 提案机制、GPT 综合机制以及任何其他治理模型链全部取消。专家任务启动前的治理模型调用数必须为 `0`。

## 第二条：模型选择与公司全局唯一性

1. 同一任务内，全部初始专家和预批准恢复专家必须来自不同模型公司，实行任务级全局 `all-different` 约束。
2. 品牌别名、子品牌和同一控制公司的模型必须归并为同一公司；Provider 不得改变模型公司的归属。
3. OpenAI、Anthropic、DeepSeek、Qwen及其他公司一律作为普通动态候选，不存在固定治理公司、固定红队公司或公司级排除名单。
4. 治理中心必须先确定所有满足付费、稳定、通用文本、旗舰层级、完整基准和任务接口条件的候选，再按明确的预计输入 Token、预计输出 Token及固定请求费计算实际任务预计费用。
5. 初始团队由费用从低到高的最前若干个不同公司模型组成；恢复名单由其后的不同公司模型组成。
6. 最终综合工作必须由初始名单中的一个模型完成，不得额外增加裁判或综合调用。治理中心在已选初始名单内，将综合能力最强且满足任务条件的模型分配给最终综合工作，其余模型按费用顺序分配给其他工作。
7. 运行前必须验证名单哈希、任务计划哈希、模型身份和公司唯一性；运行后必须根据全部实际调用重新审计公司唯一性。任何重复、偷换或身份不明均失败关闭。

## 第三条：专家团组织方式

专家中心唯一正式编排库为：

```text
networkx==3.6.1
```

组织规则：

1. 网页 GPT 在票据中显式声明工作项、角色、依赖和最终工作；专家中心不得自行生成隐藏工作项。
2. 专家中心使用 `networkx.DiGraph` 校验有向无环图，并使用 `networkx.topological_generations` 生成确定性执行阶段。
3. 无依赖的工作可在同一阶段并行；有依赖的工作只能在上游严格成功后执行。
4. 每个初始模型只绑定一个工作节点；同一模型和同一公司不得跨节点复用。
5. 每个恢复模型必须在运行前一次性分配给一个具体节点，优先保护最终综合节点和关键汇聚节点；不得被多个节点竞争或重复调用。
6. 最终综合节点必须依赖所需上游结果，并负责形成唯一最终报告。
7. 禁止引入 LangChain、AutoGen、CrewAI、Prefect、Dagster或其他 Agent/工作流框架。不得建立自主 Agent 循环、模型互评循环、递归修订或后台常驻服务。

## 第四条：Provider、恢复和失败策略

1. 模型身份由治理名单固定；专家中心只能为该固定模型选择满足任务要求的精确 Provider 端点。
2. 所有专家模型，包括 OpenAI 和 Anthropic 模型，均必须使用合格的 ZDR 端点。
3. 每次请求必须使用单一精确 Provider 锁：`only` 与 `order` 只能包含同一端点，`allow_fallbacks=false`。
4. 禁止 OpenRouter Provider fallback，禁止模型身份 fallback，禁止名单外模型，禁止自动选择第二便宜模型。
5. 模型失败时，只允许使用治理名单中预批准并已分配给该节点的恢复模型；恢复额度用完后失败关闭。
6. 禁止自动重新执行整个任务，禁止无限重试。正式模型请求的普通重试次数为 `0`。
7. 正式任务默认要求完整成功；节点、依赖、公司、证据、请求和报告任一生产门失败，均不得伪报成功。

## 第五条：费用、Token与性价比

1. 选组目标是在满足任务合同和旗舰资格的前提下，选择实际任务预计费用最低的不同公司模型组合。
2. 预计费用必须按任务画像计算：

```text
预计任务费用
= 输入单价 × 预计输入 Token / 1,000,000
+ 输出单价 × 预计输出 Token / 1,000,000
+ 每请求固定费用
```

3. 预计输入和输出 Token必须随任务明确记录，不得仅依据模型目录综合价格顺序。
4. 费用异常值只用于告警和审计，不得在用户已授权后仅因本地费用估算超过阈值而篡改有效结果。
5. 模型和Provider客观上下文窗口、输出限制及接口能力可作为兼容条件；不得用隐藏本地硬门任意截断任务。
6. 提示词必须要求结论优先、去重、信息密度优先，但不得为节省 Token 省略必要结论、证据边界或不确定性。
7. 所有预计费用、实际费用、输入输出 Token和调用数必须进入Artifact。

## 第六条：测试、生产资格与审计

1. 开发、维护和回归先执行零模型调用的确定性测试、双运行环境测试、随机压力测试和静态安全测试。
2. 付费测试只允许在用户明确授权的有限 Canary 或正式任务中执行；不得通过普通 PR 或周期检查重复扣费。
3. 移动 `production` 前必须同时满足：
   - 目标 SHA 的全部零费用 CI 通过；
   - 治理中心名单签发合同通过；
   - 专家中心 NetworkX DAG、公司唯一性、精确Provider、无工具和无Claude机制测试通过；
   - 经明确授权的真实付费端到端验收通过；
   - 独立Artifact复算通过；
   - 最终Attestation与同一Run、SHA、Artifact ID和Digest绑定；
   - 无未解决的阻断性审查线程。
4. 实际运行必须记录：治理名单哈希、治理提交、模型与公司、工作分配、精确Provider、调用次数、费用、恢复、节点结果、最终报告和证据哈希。
5. 目录选择阶段必须保持模型调用数 `0`、费用 `0`、Secret暴露 `false`。
6. 生产审计不得继续要求或伪造 GPT/Claude 治理调用；相关调用数必须明确为 `0`。

## 第七条：全面禁止工具与越权

1. 初始专家、恢复专家和最终综合专家均不得调用任何外部工具。
2. 禁止浏览器、联网搜索、MCP、插件、代码执行器、Shell、数据库查询、文件检索、连接器、外部 API及任何等效工具调用。
3. 请求体不得向模型暴露 `tools`、`tool_choice`、函数调用或其他工具能力字段。
4. 模型只能使用本次任务票据、已提供的证据包和合法依赖节点输出；不得自行访问其他中心、仓库、Secret或外部数据。
5. 模型不得提交代码、修改分支、合并PR、发布、回滚、修改Secret或派发业务中心。
6. 专家中心不得保留跨任务历史权重、偏好、记忆或上次团队状态；每个任务均从当次票据和实时目录重新开始。
7. 任何工具意图、未经提供的外部事实、越权请求、Provider偷换、名单偷换或证据不可审计，均失败关闭。
