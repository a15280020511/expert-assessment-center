# 专家研判中心最高宪法

本文件是专家研判中心的最高治理规则。代码、工作流、测试、维护、验收和生产执行均不得绕过本文件。任何冲突均按本文件处理，并以失败关闭。

## 第一条：免费优先测试，付费仅用于正式执行

1. 代码开发、维护、排错、回归、压力测试、Canary 和日常验收，必须先执行零调用确定性测试，再执行免费模型测试。
2. 免费模型测试必须使用明确标记为免费、且实际费用为 `0.0 USD` 的模型或路由；任何正费用、费用缺失或费用不可核验均视为失败。
3. 付费模型只允许用于：
   - 已通过全部免费门后的正式生产任务；
   - 用户明确、一次性授权的最终付费生产资格验收。
4. 免费门未通过、免费证据缺失、目标 SHA 不一致、Artifact 不完整或用户未明确授权时，禁止触发付费模型。
5. 付费失败后禁止自动重新执行整个任务；必须记录失败回执。仅允许在同一任务已批准的有限恢复调用额度内执行预先规划的恢复候选，不得追加无限重试。
6. 免费模型测试不得冒充正式模型身份验收，不得据此直接移动 `production`。

## 第二条：专家模型公司必须全局不同

1. 同一任务内，全部初始专家和恢复专家必须来自不同模型公司，实行任务级全局 `all-different` 约束。
2. 任何两个专家不得使用同一公司旗下模型；品牌别名、子品牌和同一控制公司的模型必须归并为同一公司。
3. OpenAI 与 Anthropic 已用于治理链，不得担任专家。
4. Provider 不得改变模型公司的归属；切换 Provider 不能规避公司唯一性。
5. 运行前必须对计划图校验公司唯一性，运行后必须根据全部实际调用模型重新审计公司唯一性；失败、超时、限流和恢复调用均不得从审计中删除。
6. 初始选择、恢复替换或实际调用结果出现跨节点公司重复时，任务必须失败关闭，不得降级放行。

## 第三条：固定治理链与有条件降级交付

```text
GPT latest 提案一次
→ Claude Opus latest 红队建议一次
→ GPT latest 综合一次
→ 确定性宪法校验
→ 不同公司的专家执行
→ 审计、Artifact、Attestation
```

1. 禁止第二次 Claude、模型循环、递归修订、Provider fallback 和无证据放行。治理链与专家执行的工具禁令依第七条执行。
2. 正式生产任务默认允许“降级成功”，用户明确要求完整交付或明确禁止降级时除外。降级成功的目的，是在部分 Provider 超时、限流或个别非关键节点质量不合格时，避免为了追求形式上的全节点成功而重复付费。
3. 降级成功必须同时满足：
   - 最终报告真实存在且达到任务最低可用交付合同；
   - 必需工作覆盖率达到机器宪法规定的最低门槛；
   - 至少有一个严格成功的有效内容节点；
   - 所有被标记为不可降级的关键工作均已覆盖；
   - 公司唯一性、禁止工具、证据边界、Provider 锁定、费用账本和调用账本全部通过；
   - 未覆盖工作、失败节点、降级节点、恢复情况和不确定性必须在报告与 Artifact 中明确披露。
4. 降级成功不得伪装为 `full_success`，必须使用独立状态 `degraded_success`；不得把缺失事实、失败节点或未经验证的内容补写为成功结果。
5. 已达到降级成功门槛后，系统不得为了把状态美化为完整成功而自动增加新的付费调用；是否追加新任务由用户另行决定。

## 第四条：生产资格

只有同时满足以下条件才可移动 `production`：

- 目标 SHA 的零费用 CI 全部通过；
- 免费模型 Canary 通过且费用严格为零；
- 公司全局唯一性在计划阶段和实际执行阶段均通过；
- 经明确授权的正式付费验收通过；
- 独立 Artifact 复算通过；
- 最终 Attestation 与同一 Run、SHA、Artifact ID 和 Digest 绑定；
- 无未解决的阻断性审查线程。

任何一项缺失，生产资格均为 `BLOCKED`。生产资格验收本身仍要求完整通过，不得用普通任务的降级成功替代正式生产晋级证据。

## 第五条：Token 与费用实行软治理，不得作为任务硬门

1. GPT 治理链、Claude 红队链和专家执行链不得以本地固定 `max_tokens`、`max_completion_tokens`、推理 Token 配额或类似字段强制截断任务。输出长度、推理强度和上下文使用必须依据任务合同、模型能力和实际需要动态决定。
2. 模型或 Provider 的客观上下文窗口、接口协议上限和服务端能力边界可以作为兼容性条件，但不得伪装成中心自行设定的 Token 预算。
3. 预计费用、风险调整费用、实际费用和费用异常阈值只用于选型参考、提示词约束、监测、告警、审计和回执；不得仅因超过本地费用阈值而拒绝计划、停止已经授权的任务、把有效结果改判失败或触发降级。
4. 所有模型提示词必须要求：先满足任务合同和证据要求，再以简洁、去重、结论优先、信息密度优先的方式完成；不得为了节省 Token 省略必要结论，也不得用重复叙述、无关展开或冗长修饰浪费 Token。
5. 免费优先测试、付费授权、固定治理链、单任务纪律、有限重试、超时保护、禁止模型循环和第七条禁止工具等安全规则继续有效。这些规则用于防止失控执行，不属于 Token 或费用硬限制。
6. 费用和 Token 必须完整记录并进入 Artifact；软治理失效时应产生告警和改进建议，但任务不得仅因本地资源估算超标而失败关闭。

## 第六条：具体问题具体分析，全部可动态项动态决定，性价比优先

1. 总原则是“具体问题具体分析、具体选择专家、具体组合专家、具体组织执行、具体匹配模型与参数”。每个任务必须从原始任务、明确约束、交付合同和当次完整可用模型端点目录重新计算，不得沿用跨任务历史编组、历史权重、固定模板或上次任务状态。
2. 所有能够根据任务动态决定的变量必须动态决定。GPT latest 是唯一的一次性动态任务拆解与专家编组器，必须依据当前具体问题动态决定：
   - 问题结构、工作项与工作粒度；
   - 工作依赖关系、执行阶段、并行或串行组织方式；
   - 专家数量、专家角色、专家职能、专家之间的协作与审查关系；
   - 最终节点、关键工作、可选工作、不可降级工作和最低可用覆盖要求；
   - 初始模型、精确 Provider、推理强度、输出容量建议；
   - 恢复候选数量、恢复候选分配、恢复优先级和停止条件。
3. 专家选择、组合、组织和参数匹配必须以“满足任务合同前提下的性价比最高”为首要目标。性价比不是单纯选择最低价格，而是在任务适配度、推理与输出能力、端点可靠性、失败概率、上下文能力、Provider 可用性、预期调用次数和实际价格之间进行任务专属权衡，以尽量少的有效调用取得足够可靠的交付。
4. 质量、可靠性和任务覆盖是性价比计算的必要组成部分。明显更便宜但高概率超时、限流、空答、质量门失败或导致重复恢复的端点，不得被视为高性价比；明显增加费用但不能增加必要交付价值的冗余专家、重复节点和重复审查也不得加入。
5. 禁止固定专家席位、固定专家数量、固定职业、固定模型组合、固定 Provider 路由、固定参数档位、关键词分支、领域硬编码、静态评分公式、预设权重、贪心组合、Pareto 剪枝、CP-SAT 或其他本地预设选模算法替代任务驱动的动态匹配。
6. 模型和 Provider 只能从当次完整、可核验且满足任务客观能力、隐私和接口条件的端点目录中选择。不得使用目录外模型，不得通过 Provider fallback 偷换模型或公司。
7. 模型参数必须根据具体任务复杂度、交付要求、节点职责和端点真实支持能力动态匹配；不得对所有任务套用同一组固定参数。Token 与费用仍按第五条实行软治理。
8. Claude 只执行一次红队建议；GPT latest 只执行一次综合。确定性代码只能校验宪法、Schema、目录成员、公司唯一性、依赖图、恢复额度和交付合同，不得重新设计、替换或静态重算专家组合。
9. 动态编组结果如不满足公司全局唯一性、端点兼容性、完整工作覆盖设计、依赖闭合或最终交付合同，必须失败关闭，不得退回固定组合或旧架构。

## 第七条：治理链和专家执行全面禁止使用工具

1. GPT 提案、Claude 红队、GPT 综合、初始专家和恢复专家均不得调用任何外部工具。
2. 禁止浏览器、联网搜索、MCP、插件、代码执行器、Shell、数据库查询、文件检索、连接器、外部 API 及任何等效工具调用。
3. 模型只能使用本次任务票据、已提供的证据包、当次模型端点目录以及同一任务治理链或依赖节点已经产生的合法输出。
4. 专家不得自行补充外部事实、抓取数据、访问其他中心或绕过 GPTs 单线接口。需要外部数据时，必须由任务发起方事先通过证据与数据中心形成可审计证据包后再提交。
5. 请求体不得向模型暴露 `tools`、`tool_choice`、函数调用或其他工具能力字段；响应中出现工具调用意图、工具调用结果或无法审计的外部信息来源时，必须失败关闭。
6. 禁止工具是系统隔离和证据边界，不属于 Token 或费用限制，不得以提高质量、降低成本、恢复失败或模型建议为由绕过。
