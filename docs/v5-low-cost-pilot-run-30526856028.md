# V5低成本试运行 Run 30526856028 复盘

## 结论

Issue #32 在多样性保留剪枝和动态预算均启用后，仍在模型调用前停止：

- 全仓单测、Ruff、编译与依赖检查通过；
- OpenRouter额度预检通过；
- V5初始规划额度0.30 USD；
- 移除预算后仍为CP-SAT `INFEASIBLE`；
- 模型调用0次；
- 实际费用0 USD；
- 生产入口未改变。

这排除了以下原因：

- 0.12 USD单策略预算过低；
- 普通Pareto剪枝删除不同模型；
- OpenRouter Provider调用失败；
- 模型答案质量门失败。

剩余原因是严格低价候选市场内，至少一个原子工作或独立副本缺少足够的模型与Provider Endpoint覆盖。

## 修复方案

### 1. 零调用候选结构报告

在CP-SAT失败时重新生成候选图并记录：

- 每个解释方案；
- 每个原子工作和副本；
- 候选数量；
- 不同模型数量；
- 不同Provider Endpoint数量；
- 无候选副本；
- 独立副本所需模型/Endpoint缺口；
- Pareto前后候选数。

结构报告不调用模型、不产生费用。

### 2. 分级候选市场

只在前一层结构不可行且调用数仍为0时扩展：

| 层级 | 输入价格上限 USD/M | 输出价格上限 USD/M |
|---|---:|---:|
| strict-low-cost | 1.50 | 4.00 |
| expanded-value | 3.00 | 10.00 |
| bounded-capability | 5.00 | 15.00 |

价格上限同时施加到模型目录和真实Provider Endpoint行，可靠性最低0.80。

### 3. 不变硬门

- 总实际费用上限0.50 USD；
- V5规划额度最高0.35 USD；
- 总调用上限40；
- 每次直接调用最坏费用预检；
- HTTP 402全局停止；
- 能力硬门槛不降低；
- 模型与Provider独立性不降低；
- 工作单元和质量要求不降低；
- 生产切换资格固定为false。

如果三层市场均不可行，结构报告将成为下一步修复依据，不再盲目扩大价格或重复付费测试。

## 证据

- Issue：#32；
- Run：`30526856028`；
- Artifact：`8753032500`；
- Artifact digest：`1a52d4639ae8503c47d73bd141450247b8aae71e8c418256d5edccfd070e9fce`；
- 模型调用：0；
- 实际费用：0 USD。
