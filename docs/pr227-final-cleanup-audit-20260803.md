# PR #227 最终清理与颗粒化审计记录

日期：2026-08-03

## 修复范围

- 统一 JSON 读写、有限数值解析、完成原因解析和文件摘要等确定性辅助实现。
- 保留 `ExecutionEngine` 的静态兼容入口，消除子类继承回归。
- 删除永久停用的旧付费验收工作流、固定参数模板和旧可靠性账本 Schema。
- 删除一次性审计载荷与自提交工作流，不在生产仓库保留临时执行资产。
- 更新清理回归与工作流合同测试，使票据、运行时和文档只暴露实际生效的接口。

## 基础验证结果

- Python 全量编译：PASS。
- 八个随机种子的满载与极端压力矩阵：PASS。
- 八个随机种子的同进程随机重排：PASS。
- 旧规划文件与旧工作流残留：0。
- 旧求解器、本地任务计算与优化符号：0。
- 模块级属性改写：0。
- 完全重复函数体组：0。
- 仓库逐行审计：Critical 0、High 0、Medium 0。

## 首次真实付费验收发现与修复

- 真实受限验收 Run：`30770191830`。
- 目标源码：`6cf0661580d59bd62dc37a6a948920757bf70e41`。
- 密钥检查、锁定依赖、Ruff、全量编译和 281 项回归均通过。
- 运行在 GPT 提案之后、Claude 红队调用之前 fail-closed。
- 根因：GPT 输出合同允许 `functions` 使用受限短文本，但 Claude 输入校验错误地要求纯 ASCII 标识符，形成跨阶段接口合同不一致。
- 修复提交：`bb8521a621a94ceeb39d5f5455053eb819a85ca4`。
- 修复后统一为长度受限、禁止控制字符、禁止重复的短文本列表，支持中文及包含空格的任务函数描述。
- 新增中文函数描述通过和重复函数描述拒绝两项回归。
- 首次失败 Artifact ID：`8840260084`；由于失败发生在治理账本完成前，不能从该 Artifact 给出可核验的精确实际费用。

## 第二次真实付费验收发现与跨阶段合同加固

- 真实受限验收 Run：`30770782954`。
- 目标源码：`f7f0bec6cbecee2314a352a736f45f92d82034f4`。
- 密钥检查、锁定依赖、Ruff、全量编译和 283 项回归均通过。
- 首次 `functions` 修复生效：执行已通过 GPT 提案并进入 Claude 单次红队。
- 运行在 Claude 返回结构化建议后、GPT 综合调用之前 fail-closed。
- 根因：Claude 建议中的 `target` 是人类可读的咨询目标，但响应 Schema 和解析器错误地要求纯 ASCII 标识符。
- 第二次失败 Artifact ID：`8840452885`，SHA-256：`a1a8c8e42a9ff392e1ded7c1cf3a6897ffd2297019c130cfccd33133605b4fe1`。
- 由于失败发生在完整治理账本生成前，不能从部分 Artifact 给出可核验的精确实际费用。

本轮不只修改 `target`：

- Claude 建议 `target` 统一为长度受限、禁止控制字符的自然语言短文本，并在解析后去除首尾空白。
- GPT 提案的 `work_id`、`node_id`、模型、Provider、恢复端点、依赖引用、边端点和最终节点由解析器按与 JSON Schema 相同的标识符规则强制校验。
- 边 `relation_type` 由解析器按同一枚举强制校验。
- `required_outputs`、`functions`、依赖和节点工作分配的唯一性在解析阶段 fail-closed，避免到 Claude 或物化阶段才暴露合同漂移。
- 新增中文咨询目标、控制字符拒绝、中文描述与内部 ID 分离、非法模型 ID、未知关系类型和重复输出字段回归。

跨阶段合同加固提交：`a193ee05031005fa1ab6871562a990b20103abc5`。

该提交的专用修复门结果：

- Ruff：PASS。
- Python 全量编译：PASS。
- 聚焦合同测试：15/15 PASS。
- 完整单元回归：290/290 PASS。
- 8 个随机种子 × 23 项同进程合同与极端压力：184/184 PASS。
- Critical：0。
- High：0。
- Medium：0。

## 边界

`main` 与 `production` 尚未移动。加固后的真实受限付费端到端重验仍需同时获得完整调用账本、主 Artifact、独立复算和最终 Attestation PASS，才能解除生产阻断。
