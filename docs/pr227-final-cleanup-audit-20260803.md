# PR #227 最终清理与颗粒化审计记录

日期：2026-08-03

## 修复范围

- 统一 JSON 读写、有限数值解析、完成原因解析和文件摘要等确定性辅助实现。
- 保留 `ExecutionEngine` 的静态兼容入口，消除子类继承回归。
- 删除永久停用的旧付费验收工作流、固定参数模板和旧可靠性账本 Schema。
- 删除一次性审计载荷与自提交工作流，不在生产仓库保留临时执行资产。
- 更新清理回归与工作流合同测试，使票据、运行时和文档只暴露实际生效的接口。

## 验证结果

- Python 全量编译：PASS。
- 全量单元回归：281/281 PASS。
- 八个随机种子的满载与极端压力矩阵：PASS。
- 八个随机种子的同进程随机重排：PASS。
- 旧规划文件与旧工作流残留：0。
- 旧求解器、本地任务计算与优化符号：0。
- 模块级属性改写：0。
- 完全重复函数体组：0。
- 仓库逐行审计：Critical 0、High 0、Medium 0、Low 0、Info 0。

## 首次真实付费验收发现与修复

- 真实受限验收 Run：`30770191830`。
- 目标源码：`6cf0661580d59bd62dc37a6a948920757bf70e41`。
- 密钥检查、锁定依赖、Ruff、全量编译和 281 项回归均通过。
- 运行在 GPT 提案之后、Claude 红队调用之前 fail-closed。
- 根因：GPT 输出合同允许 `functions` 使用受限短文本，但 Claude 输入校验错误地要求纯 ASCII 标识符，形成跨阶段接口合同不一致。
- 修复提交：`bb8521a621a94ceeb39d5f5455053eb819a85ca4`。
- 修复后统一为长度受限、禁止控制字符、禁止重复的短文本列表，支持中文及包含空格的任务函数描述。
- 新增中文函数描述通过和重复函数描述拒绝两项回归。
- 修复门已通过 Ruff、Python 全量编译、聚焦回归、完整回归和 Medium 阻断审计。
- 首次失败 Artifact ID：`8840260084`；由于失败发生在治理账本完成前，不能从该 Artifact 给出可核验的精确实际费用。

## 边界

`main` 与 `production` 尚未移动。修复后的真实受限付费端到端重验仍需同时获得完整调用账本、主 Artifact、独立复算和最终 Attestation PASS，才能解除生产阻断。
